This is a mobile-first, reactive web application. All UI pages should be tailored to a mobile view first. Desktop view is a secondary concern; perhaps unnecessary altogether.

Use shadcn components to build the UI.

Always use kebabe case and all lowercase letters to name files.